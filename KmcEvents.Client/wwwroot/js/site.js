document.addEventListener('DOMContentLoaded',()=>{const b=document.getElementById('menuBtn'),n=document.getElementById('mainNav');if(b&&n)b.addEventListener('click',()=>n.classList.toggle('active'));document.querySelectorAll('.toggle-password').forEach(btn=>btn.addEventListener('click',()=>{const el=document.getElementById(btn.dataset.target);if(!el)return;el.type=el.type==='password'?'text':'password';btn.textContent=el.type==='password'?'Show':'Hide';}));document.querySelectorAll('input[name=CardNumber]').forEach(i=>i.addEventListener('input',()=>i.value=i.value.replace(/\D/g,'').slice(0,16)));});
